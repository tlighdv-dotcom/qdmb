import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=(path)=>fs.readFileSync(new URL(`../${path}`, import.meta.url),'utf8');
test('public page exposes required DOM hooks and loads Supabase v2',()=>{const html=read('index.html');for(const id of ['memberGrid','searchInput','memberCount','statusBox','recentActivity','imageModal','imageModalImg','summaryWithFriends','friendGroups'])assert.match(html,new RegExp(`id=["']${id}["']`));assert.match(html,/@supabase\/supabase-js@2/);assert.match(html,/assets\/public\.js/)});
test('config contains browser-safe placeholders only',()=>{const config=read('config.js');assert.match(config,/SUPABASE_URL/);assert.match(config,/SUPABASE_KEY/);assert.doesNotMatch(config,/ADMIN_EMAIL/);assert.match(config,/sb_publishable_/);assert.doesNotMatch(config,/service_role/i);assert.doesNotMatch(config,/ADMIN_PASSWORD/i)});
test('admin page exposes login CRUD status and friend hooks',()=>{const html=read('admin.html');for(const id of ['loginForm','emailInput','passwordInput','adminPanel','memberForm','adminMemberList','logoutBtn','memberStatusInput','adminRecentActivity','imageModal','imageModalImg','hasFriendsInput','friendPicker','friendSearchInput','friendOptions','selectedFriends'])assert.match(html,new RegExp(`id=["']${id}["']`));assert.match(html,/type=["']password["']/);assert.match(html,/assets\/admin\.js/)});
test('Supabase SQL enables RLS, admin writes, status logs and friendship realtime',()=>{const sql=read('supabase.sql');for(const pattern of [/enable row level security/i,/to anon/i,/app_metadata/i,/role[^\n]*admin/i,/member-profiles/i,/storage\.objects/i,/supabase_realtime/i,/status\s+text/i,/khong_dat/i,/member_status_logs/i,/member_status_logs_member_id_idx/i,/log_member_status_change/i,/member_friends/i])assert.match(sql,pattern);assert.match(sql,/revoke\s+execute\s+on\s+function\s+public\.log_member_status_change\(\)\s+from\s+public,\s*anon,\s*authenticated/i)});
